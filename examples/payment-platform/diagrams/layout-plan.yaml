# Example layout-plan.yaml — Diagram orchestration plan
# Generated by @diagram-generator agent.
# Path: architecture/<system-id>/diagrams/layout-plan.yaml

diagram_metadata:
  system_id: payment-platform
  system_name: Payment Processing Platform
  generated_at: "2026-03-30T15:00:00Z"

levels:
  - level: context
    title: "Payment Platform — System Context"
    nodes:
      - id: payment-platform
        label: Payment Platform
        type: system
        internal: true
      - id: card-network
        label: Visa / Mastercard Network
        type: system
        internal: false
    edges:
      - source: payment-platform
        target: card-network
        label: "submits transactions to"

  - level: container
    title: "Payment Platform — Container View"
    nodes:
      - id: api-tier
        label: API Tier
        technology: Kong Gateway
        type: api_gateway
      - id: app-core
        label: Application Core
        technology: Java / Spring Boot
        type: application_server
      - id: data-tier
        label: Data Tier
        technology: PostgreSQL 15
        type: database
    edges:
      - source: api-tier
        target: app-core
        label: "routes requests to"
        data_classification: confidential
      - source: app-core
        target: data-tier
        label: "reads/writes transaction data"
        data_classification: confidential

  - level: component
    title: "Application Core — Component View"
    scope_container: app-core
    nodes:
      - id: payment-api
        label: Payment API
        technology: Spring Boot 3.2
        type: api
      - id: payment-db
        label: Payment Database
        technology: PostgreSQL 15
        type: database
    edges:
      - source: payment-api
        target: payment-db
        label: "persists transactions"
        data_classification: confidential
