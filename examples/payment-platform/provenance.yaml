# ===========================================================================
# EXAMPLE FILE — Included as a reference implementation.
# This is NOT a real production architecture. Modify or replace for your use.
# ===========================================================================
# Example provenance.yaml — Source citations for extracted entities
# Generated by @doc-extractor agent.
# Path: architecture/<system-id>/provenance.yaml
#
# Structure matches validate-provenance.py contract:
#   extraction_date, pipeline_version, documents_analyzed, entities,
#   conflicts_resolved, unresolved, statistics

extraction_date: "2026-03-30"
pipeline_version: "1.0.0"

documents_analyzed:
  - file: payment-platform-hldd-v2.pdf
    format: pdf
    pages: 42
    extraction_method: pymupdf

entities:
  - entity_type: context
    entity_id: payment-platform
    fields:
      name:
        value: Payment Platform
        confidence: HIGH
        source: "payment-platform-hldd-v2.pdf, Section 1.1"
        quote: "The Payment Platform is the core system responsible for processing card-present and card-not-present transactions."
        pass: prose
        verified: true

  - entity_type: container
    entity_id: api-tier
    fields:
      technology:
        value: Kong Gateway
        confidence: HIGH
        source: "payment-platform-hldd-v2.pdf, Section 3.2"
        quote: "All external API traffic is routed through a Kong Gateway instance providing rate limiting and authentication."
        pass: prose
        verified: true

  - entity_type: component
    entity_id: payment-db
    fields:
      technology:
        value: PostgreSQL 15
        confidence: MEDIUM
        source: "payment-platform-hldd-v2.pdf, Section 4.1"
        quote: "Transaction data is persisted in a PostgreSQL database."
        pass: prose
        verified: false

conflicts_resolved: []
unresolved: []

statistics:
  total_fields_extracted: 3
  high_confidence: 2
  medium_confidence: 1
  low_confidence: 0
  uncertain: 0
