catalog:
  type: deployments
  description: Deployment compositions combining network and product patterns
  tree:
    - name: Examples
      key: examples
      deployments:
        - id: mq-prod-us-east
          name: MQ Production US East (EXAMPLE)
          description: EXAMPLE — IBM MQ messaging on standard 3-tier network
