# GENERATED by compose.py from deployments/mq-prod-us-east/manifest.yaml
# DO NOT EDIT — modify the manifest or source patterns instead.
# Generated: 2026-04-03T21:03:16Z
dataflow_metadata:
  audience: application
dataflows:
- id: prod-web-to-app
  source_zone: prod-dmz
  target_zone: prod-private-app-tier
  source_component: prod-waf-engine
  target_component: prod-lb-engine
  target_listener_ref: prod-lb-https-in
  label: EXAMPLE — Web tier to application tier traffic
  protocol: HTTPS
  port: 443
  tls_required: true
  data_classification: confidential
  direction: ingress
  description: 'EXAMPLE — Inspected HTTPS traffic forwarded from DMZ WAF through load
    balancer to application servers.

    '
- id: prod-app-to-data
  source_zone: prod-private-app-tier
  target_zone: prod-private-data-tier
  label: EXAMPLE — Application tier to data tier queries
  protocol: TCP
  port: 5432
  tls_required: true
  data_classification: restricted
  authn_mechanism: certificate
  direction: ingress
  description: 'EXAMPLE — Database queries from application servers to PostgreSQL
    in the data tier. Uses mTLS for authentication.

    '
- id: prod-app-to-cache
  source_zone: prod-private-app-tier
  target_zone: prod-private-app-tier
  label: EXAMPLE — Application tier internal cache traffic
  protocol: TCP
  port: 6379
  tls_required: true
  data_classification: internal
  authn_mechanism: password
  direction: bidirectional
  description: 'EXAMPLE — Redis cache traffic within the application tier.

    '
- id: mq-producer-to-qm
  source_component: mq-app-producer
  target_component: mq-queue-manager
  target_listener_ref: mq-mq-listener
  label: EXAMPLE — Message producer to queue manager
  protocol: MQ
  port: 1414
  tls_required: true
  data_classification: confidential
  authn_mechanism: certificate
  direction: ingress
  description: 'EXAMPLE — Application producers send messages to the queue manager
    over MQ protocol with mTLS authentication.

    '
- id: mq-qm-to-consumer
  source_component: mq-queue-manager
  target_component: mq-app-consumer
  label: EXAMPLE — Queue manager to message consumer
  protocol: MQ
  port: 1414
  tls_required: true
  data_classification: confidential
  authn_mechanism: certificate
  direction: egress
  description: 'EXAMPLE — Queue manager delivers messages to consuming applications
    via MQ channels with certificate-based authentication.

    '
