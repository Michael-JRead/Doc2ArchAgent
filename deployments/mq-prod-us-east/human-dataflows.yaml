# GENERATED by compose.py from deployments/mq-prod-us-east/manifest.yaml
# DO NOT EDIT — modify the manifest or source patterns instead.
# Generated: 2026-04-03T21:03:16Z
dataflow_metadata:
  audience: human
dataflows:
- id: prod-user-to-web
  source_zone: prod-internet
  target_zone: prod-dmz
  label: EXAMPLE — End user HTTPS access to web tier
  protocol: HTTPS
  port: 443
  tls_required: true
  data_classification: confidential
  authn_mechanism: oidc
  direction: ingress
  session_timeout_minutes: 30
  description: 'EXAMPLE — End users access the application through HTTPS. Authenticated
    via OIDC (e.g., corporate SSO). Session timeout enforced at 30 minutes per security
    policy.

    '
- id: prod-admin-to-mgmt
  source_zone: prod-management
  target_zone: prod-private-app-tier
  label: EXAMPLE — Administrator access to management interfaces
  protocol: HTTPS
  port: 8443
  tls_required: true
  data_classification: restricted
  authn_mechanism: mfa
  admin_interface: true
  direction: ingress
  session_timeout_minutes: 15
  description: 'EXAMPLE — Administrators access management consoles via dedicated
    management network. MFA required per PCI-DSS Req 8.3. Short session timeout for
    privileged access.

    '
- id: mq-admin-to-console
  target_component: mq-mq-console
  target_listener_ref: mq-mq-console-https
  label: EXAMPLE — Administrator access to MQ web console
  protocol: HTTPS
  port: 9443
  tls_required: true
  data_classification: restricted
  authn_mechanism: mfa
  admin_interface: true
  direction: ingress
  session_timeout_minutes: 15
  description: 'EXAMPLE — MQ administrators access the web console for queue management.
    MFA required for administrative access. Short session timeout per security policy.

    '
