{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Michael-JRead/Doc2ArchAgent/schemas/manifest.schema.json",
  "title": "Doc2ArchAgent Deployment Manifest Schema",
  "description": "Schema for deployment manifests that compose network + product patterns into a deployment.",
  "type": "object",
  "properties": {
    "manifest": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$",
          "description": "Deployment identifier in kebab-case."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable deployment name."
        },
        "description": { "type": "string" },
        "environment": {
          "type": "string",
          "enum": ["development", "staging", "production", "dr"],
          "description": "Target environment."
        },
        "region": { "type": "string" },
        "status": {
          "type": "string",
          "enum": ["proposed", "approved", "active", "deprecated", "example"]
        },
        "cloud_provider": {
          "type": "string",
          "description": "Cloud provider (e.g., AWS, Azure, GCP, on-prem)."
        },
        "network": {
          "type": "object",
          "description": "Network pattern selection (exactly one). DEPRECATED: use 'networks' array instead for multi-network support.",
          "properties": {
            "pattern_ref": {
              "type": "string",
              "description": "Pattern ID referencing a network pattern directory."
            },
            "version": {
              "type": "string",
              "description": "Pinned pattern version."
            },
            "id_prefix": {
              "type": "string",
              "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$",
              "description": "Prefix applied to all network entity IDs."
            },
            "purpose": {
              "type": "string",
              "enum": ["application", "human", "hybrid", "management", "other"],
              "description": "Who/what this network serves. Agents should ask user if unclear."
            },
            "overrides": {
              "type": "object",
              "description": "Per-entity field overrides keyed by original entity ID.",
              "additionalProperties": { "type": "object" }
            }
          },
          "required": ["pattern_ref", "id_prefix"],
          "additionalProperties": true
        },
        "networks": {
          "type": "array",
          "description": "Network pattern selections (one or more). Each entry gets a unique id_prefix. Replaces singular 'network' field.",
          "items": {
            "type": "object",
            "properties": {
              "pattern_ref": {
                "type": "string",
                "description": "Pattern ID referencing a network pattern directory."
              },
              "version": {
                "type": "string",
                "description": "Pinned pattern version."
              },
              "id_prefix": {
                "type": "string",
                "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$",
                "description": "Prefix applied to all network entity IDs. Must be unique per network."
              },
              "purpose": {
                "type": "string",
                "enum": ["application", "human", "hybrid", "management", "other"],
                "description": "Who/what this network serves. Agents should ask user if unclear."
              },
              "overrides": {
                "type": "object",
                "description": "Per-entity field overrides keyed by original entity ID.",
                "additionalProperties": { "type": "object" }
              }
            },
            "required": ["pattern_ref", "id_prefix"],
            "additionalProperties": true
          },
          "minItems": 1
        },
        "products": {
          "type": "array",
          "description": "Product pattern selections (one or more).",
          "items": {
            "type": "object",
            "properties": {
              "pattern_ref": {
                "type": "string",
                "description": "Pattern ID referencing a product pattern directory."
              },
              "version": {
                "type": "string",
                "description": "Pinned pattern version."
              },
              "id_prefix": {
                "type": "string",
                "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$",
                "description": "Prefix applied to all product entity IDs. Must be unique per product."
              },
              "context_name": {
                "type": "string",
                "description": "Override display name for the product's context."
              },
              "overrides": {
                "type": "object",
                "description": "Per-entity field overrides keyed by original entity ID.",
                "additionalProperties": { "type": "object" }
              }
            },
            "required": ["pattern_ref", "id_prefix"],
            "additionalProperties": true
          },
          "minItems": 1
        },
        "placements": {
          "type": "array",
          "description": "Zone placement rules mapping containers to zones.",
          "items": {
            "type": "object",
            "properties": {
              "container_ref": {
                "type": "string",
                "description": "Prefixed container ID."
              },
              "zone_ref": {
                "type": "string",
                "description": "Prefixed zone ID."
              },
              "replicas": { "type": "integer", "minimum": 1 },
              "runtime_user": {
                "type": "string",
                "enum": ["root", "non_root", "unknown"]
              },
              "read_only_filesystem": { "type": "boolean" },
              "resource_limits_set": { "type": "boolean" },
              "network_policy_enforced": { "type": "boolean" },
              "image_signed": { "type": "boolean" }
            },
            "required": ["container_ref", "zone_ref"],
            "additionalProperties": true
          }
        },
        "cross_network_links": {
          "type": "array",
          "description": "Explicit zone connections across different network patterns. Required when zones from one pattern need to communicate with zones from another.",
          "items": {
            "type": "object",
            "properties": {
              "source_zone": {
                "type": "string",
                "description": "Prefixed zone ID from one network pattern."
              },
              "target_zone": {
                "type": "string",
                "description": "Prefixed zone ID from another network pattern."
              },
              "direction": {
                "type": "string",
                "enum": ["ingress", "egress", "bidirectional"],
                "description": "Traffic direction from source to target."
              },
              "description": {
                "type": "string",
                "description": "Human-readable description of this cross-network link."
              }
            },
            "required": ["source_zone", "target_zone", "direction"],
            "additionalProperties": true
          }
        },
        "cross_product_relationships": {
          "type": "array",
          "description": "Relationships connecting components across different products.",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "source_component": { "type": "string" },
              "target_component": { "type": "string" },
              "target_listener_ref": { "type": "string" },
              "label": { "type": "string" },
              "synchronous": { "type": "boolean" },
              "data_classification": { "type": "string" },
              "data_entities": {
                "type": "array",
                "items": { "type": "string" }
              }
            },
            "required": ["id", "source_component", "target_component"],
            "additionalProperties": true
          }
        }
      },
      "required": ["id", "name", "environment", "products"],
      "oneOf": [
        { "required": ["network"] },
        { "required": ["networks"] }
      ],
      "additionalProperties": true
    }
  },
  "required": ["manifest"],
  "additionalProperties": true
}
